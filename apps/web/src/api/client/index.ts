// Use axios for client-side fetching because it is faster and easier to use
// Create all the query-hooks here and their respective toasts and functions
// Mostly use these for mutations
// Ideally use cookies instead of localStorage
